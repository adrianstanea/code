services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace:cached # Your code synced between host and container
    working_dir: /workspace
    cap_add:
      - SYS_PTRACE # Enable GDB/LLDB debugging
    security_opt:
      - seccomp:unconfined
    command: [ "sleep", "infinity" ] # Keep container alive for VS Code
